apiVersion: v1
kind: Service
metadata:
  name: employee-api-service
  namespace: employee-ns
spec:
  type: LoadBalancer
  selector:
    app: employee-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP

